# The published pages carry sha256 CSP hashes over their own inline <script>
# blocks. A CRLF working tree changes those bytes and breaks tools/check-csp.mjs,
# so text files stay LF everywhere regardless of core.autocrlf.
* text=auto eol=lf

*.png binary
